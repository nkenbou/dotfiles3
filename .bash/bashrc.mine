# functions の読み込み
if [ -d ~/.bash/functions ]; then
    for func in ~/.bash/functions/*; do
        [ -f "$func" ] && source "$func"
    done
fi

bind -x '"\300": ghql'
bind '"\C-g":"\300\C-m"'

# mise
eval "$(mise activate bash)"
